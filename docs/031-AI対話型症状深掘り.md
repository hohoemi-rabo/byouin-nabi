# 031: AI対話型症状深掘り

**フェーズ**: Phase 2-B（025の後）
**優先度**: 中
**依存**: 016（Gemini AI統合）, 025（結果ページ強化）
**参照**: Phase2-Requirements.md セクション 4

---

## 概要

基本7問のアンケート回答後、AIが症状に応じた追加質問（2-3問）を生成し、対話形式で情報を深掘りする。これにより、より精度の高い緊急度判定・診療科推奨を実現する。

**シニアファースト**: 基本7問はそのまま維持（簡単・ボタン式）。追加質問はオプショナルで、「もっと詳しく調べる」ボタンを押した場合のみ発動。

---

## Todo

### AI追加質問生成

- [ ] `/api/symptoms/follow-up/route.ts` — 追加質問生成APIルート
  - リクエスト: 基本7問の回答（QuestionnaireData）+ 初回AI判定結果
  - Gemini API に「この症状を深掘りするための質問を2-3個生成」を指示
  - レスポンス: `{ questions: [{ id, text, type: 'select' | 'text', options?: string[] }] }`
  - 例: 「おなかのどのあたりが痛みますか？」→ 選択肢: 上腹部/下腹部/右側/左側/全体

### 追加質問UIコンポーネント

- [ ] `src/components/SymptomResult/FollowUpChat.tsx` — 対話型UIコンポーネント
  - チャット風のバブル表示（AI質問 → ユーザー回答）
  - 選択式回答: ボタン式（基本アンケートと同じQuestionOptionスタイル）
  - テキスト回答: 簡単な入力フィールド
  - 「スキップ」ボタン（答えたくない質問を飛ばせる）
  - 最大3問まで（それ以上は追加しない）

### AI再判定

- [ ] `/api/symptoms/ai-recommend` を拡張 — 追加回答を含めた再判定
  - リクエストに `follow_up_answers` フィールドを追加
  - 追加情報を含めたプロンプトでより詳細な判定
  - 緊急度・推奨診療科の精度向上

### 結果ページへの統合

- [ ] `/results` ページに「もっと詳しく調べる」ボタン追加
  - 初回AI判定の後に表示
  - ボタン押下 → FollowUpChat コンポーネント表示
  - 追加質問に全て回答（またはスキップ）→ AI再判定 → 結果更新
- [ ] 再判定結果で UrgencyBadge と推奨診療科を更新

### 追加質問の例（AIが生成）

| 基本回答 | AIが生成する追加質問 |
|---------|-------------------|
| おなかが痛い | 「痛みの場所は？」（上腹部/下腹部/右/左/全体） |
| おなかが痛い + 熱がある | 「吐き気はありますか？」（はい/いいえ） |
| あたまが痛い | 「痛みの種類は？」（ズキズキ/締め付け/片側だけ/全体） |
| むねが痛い | 「安静時にも痛みますか？」（はい/いいえ） |
| あしが痛い + 高齢 | 「歩くと痛みが増しますか？」（はい/いいえ） |

### 型定義

- [ ] `src/types/ai.ts` に追加
  - `FollowUpQuestion` — AI生成の追加質問
  - `FollowUpAnswer` — ユーザーの追加回答

### 確認

- [ ] 追加質問の生成確認（症状パターン別に3-5ケース）
- [ ] 再判定の精度向上確認（追加情報で結果が変わることを確認）
- [ ] スキップ機能の動作確認
- [ ] シニア向けUI確認（ボタンサイズ、文字サイズ）
- [ ] `npm run build` でエラーなし確認

---

## 技術メモ

- Gemini API の1回目: 緊急度判定（既存）、2回目: 追加質問生成、3回目: 再判定
- API コスト: 1ユーザーあたり最大3回のAPI呼び出し（gemini-3.1-flash-lite-preview なら低コスト）
- 追加質問は Gemini に JSON で返させる（`responseMimeType: "application/json"`）
- 基本7問の回答は変更しない（既存フローを壊さない）
- 「もっと詳しく調べる」はオプション（押さなくても初回判定で十分使える）
