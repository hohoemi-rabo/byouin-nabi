# 018: 交通データ管理・管理画面

**フェーズ**: Phase 2-A
**優先度**: 高
**依存**: 015（交通関連テーブル作成後）
**参照**: Phase2-Requirements.md セクション 3.1, 8.1, 8.2

---

## 概要

交通サービス（路線バス・デマンド交通・タクシー等）のデータを管理画面から CRUD 操作できるようにする。
Phase 1 の病院管理画面と同様のパターンで実装。

---

## Todo

### 管理画面サイドバー更新

- [x] `AdminSidebar` に「🚌 交通手段管理」メニュー項目を追加（`/admin/transport`）

### 交通サービス一覧（`/admin/transport`）

- [x] ページ作成（Client Component）
- [x] `/api/transport` から全交通サービスを取得・表示
- [x] サービス種別フィルター（路線バス / デマンド / タクシー / 福祉タクシー / 送迎）
- [x] 各サービスカード: 名称・事業者・種別・対応エリア・連絡先
- [x] 編集・削除ボタン（Phase 1 の病院一覧と同様のUI）
- [x] 削除時の ConfirmModal

### 交通サービス登録・編集

- [x] `src/components/Admin/TransportForm.tsx` — 登録/編集共用フォーム
- [x] `/admin/transport/new/page.tsx` — 新規登録ページ
- [x] `/admin/transport/[id]/edit/page.tsx` — 編集ページ

### Server Actions

- [x] `src/app/admin/transport-actions.ts` に Server Actions 作成
  - createTransportService / updateTransportService / deleteTransportService
  - importTransportServices / exportTransportServicesCSV

### API ルート

- [x] `GET /api/transport/route.ts` — 交通サービス一覧取得（area/type フィルタ対応）
- [x] `GET /api/transport/[id]/route.ts` — 交通サービス詳細取得

### CSVインポート/エクスポート

- [x] `/admin/transport/import/page.tsx` — インポート画面
- [x] Phase 1 の病院インポートと同じUI・ロジックを再利用
- [x] CSVヘッダー定義: サービス名, 事業者, 種別, 対応エリア, 電話番号, ...

### 初期データ投入

- [x] 南信地域の交通サービスデータを準備（CSV 21件）
- [x] インポート実行（路線バス2、デマンド14、タクシー3、福祉タクシー1、送迎1）

### 確認

- [ ] CRUD 全操作の動作確認
- [ ] CSVインポート/エクスポートの動作確認
- [x] `npm run build` でエラーなし確認

---

## 技術メモ

- Phase 1 の病院管理画面（`/admin/hospitals`）のパターンを踏襲
- `service_area` は TEXT[] 型 → GIN インデックスで overlaps 検索
- 全操作は `supabaseAdmin`（Service Role Key）経由
- `verifyAdminAuth()` を全 Server Actions で実行
