# 015: DB拡張・マイグレーション（Phase 2 基盤）

**フェーズ**: Phase 2-A
**優先度**: 最高（他チケットの前提）
**依存**: なし（Phase 1 完了済み前提）
**参照**: Phase2-Requirements.md セクション 2.2, 9

---

## 概要

Phase 2 で必要な全テーブルの作成・既存テーブルの拡張を行う。
全チケットの土台となるため、最初に完了させる。

---

## Todo

### hospitals テーブル拡張

- [x] ALTER TABLE で以下のカラムを追加（マイグレーションSQL作成）
  - `latitude` NUMERIC — 緯度
  - `longitude` NUMERIC — 経度
  - `online_consultation` BOOLEAN DEFAULT false — オンライン診療対応
  - `online_consultation_url` TEXT nullable — オンライン診療予約URL
  - `parking` BOOLEAN DEFAULT false — 駐車場の有無
  - `parking_capacity` INTEGER nullable — 駐車場台数
  - `barrier_free` BOOLEAN DEFAULT false — バリアフリー対応
  - `emergency_available` BOOLEAN DEFAULT false — 救急対応の可否
  - `shuttle_bus` BOOLEAN DEFAULT false — 送迎バスの有無
  - `shuttle_bus_info` TEXT nullable — 送迎バスの詳細情報
- [x] Hospital 型定義（`src/types/hospital.ts`）を更新

### 交通関連テーブル作成

- [x] `transport_services` テーブル作成（GINインデックス含む）
- [x] `bus_routes` テーブル作成
- [x] `bus_stops` テーブル作成（位置インデックス含む）
- [x] `bus_timetables` テーブル作成（複合インデックス含む）
- [x] 交通関連の TypeScript 型定義を `src/types/transport.ts` に作成

### ユーザー関連テーブル作成

- [x] `profiles` テーブル作成（auth.users への FK）
- [x] `favorite_facilities` テーブル作成（UNIQUE制約含む）
- [x] `search_history` テーブル作成
- [x] `visit_reminders` テーブル作成
- [x] `family_links` テーブル作成
- [x] ユーザー関連の TypeScript 型定義を `src/types/user.ts` に作成

### その他テーブル作成

- [x] `facilities` テーブル作成（お出かけナビ用、インデックス含む）
- [x] `search_logs` テーブル作成（匿名統計用、インデックス含む）
- [x] 施設関連の TypeScript 型定義を `src/types/facility.ts` に作成

### RLS ポリシー設定

- [x] `profiles` — 自分のデータのみ SELECT/INSERT/UPDATE
- [x] `favorite_facilities`, `search_history`, `visit_reminders` — 自分のデータのみ全操作
- [x] `family_links` — elder/family それぞれのポリシー（複数permissive解消済み）
- [x] `transport_services`, `bus_routes`, `bus_stops`, `bus_timetables`, `facilities` — Public read / Admin write
- [x] `search_logs` — Anyone INSERT / Admin SELECT
- [x] 全ポリシーで `(select auth.uid())` / `(select auth.role())` のinitplanキャッシュ化パターンを使用
- [x] 全FK列にインデックスを追加（Supabase Advisor 指摘対応）

### 確認

- [x] Supabase MCP で全マイグレーション適用確認（7マイグレーション適用済み）
- [x] Supabase Advisor でセキュリティ ERROR 0件を確認
- [x] Supabase Advisor でパフォーマンス WARN 0件を確認（INFO: unused_index のみ — テーブル空のため）
- [x] `npm run build` で型エラーなしを確認

---

## 技術メモ

- マイグレーション方針: ALTER TABLE でカラム追加。全て nullable or DEFAULT 値ありなので既存データに影響なし
- Supabase プロジェクトID: `xsydqbczmzfufeywjfps`（`taihei-studio` は絶対に操作禁止）
- RLS の `auth.role()` → `(select auth.role())` キャッシュ化パターンを徹底
- 全タイムスタンプは `TIMESTAMPTZ` を使用
