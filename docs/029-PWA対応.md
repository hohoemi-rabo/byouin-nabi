# 029: PWA 対応

**フェーズ**: Phase 2-C
**優先度**: 低
**依存**: 全主要機能の実装完了後
**参照**: Phase2-Requirements.md セクション 15.2

---

## 概要

Progressive Web App 化により、ホーム画面追加・オフラインキャッシュ・Service Worker を実装。

---

## Todo

### PWA 基盤

- [x] `@ducanh2912/next-pwa` パッケージをインストール（App Router 対応版）
- [x] `public/manifest.json` — アプリ名・テーマカラー・アイコン（192x192, 512x512）
- [x] `next.config.ts` に withPWA ラッパー追加（dev 環境では無効）
- [x] `src/app/layout.tsx` に manifest + appleWebApp メタデータ追加

### Service Worker

- [x] next-pwa が自動生成（Workbox ベース）
- [x] オフラインフォールバック: `/~offline` に自動リダイレクト
- [x] .gitignore に sw.js / workbox-*.js 等の生成ファイルを追加

### オフラインページ

- [x] `src/app/~offline/page.tsx` — 119番ボタン + 再読み込みボタン

### 確認

- [ ] ホーム画面追加のフロー確認（本番デプロイ後）
- [ ] オフライン時のフォールバック確認（本番デプロイ後）
- [x] `npm run build` でエラーなし確認

---

## 技術メモ

- `next-pwa` で Service Worker を自動生成
- オフラインでも緊急時の電話番号（119番、かかりつけ医）にアクセスできることが最重要
- iOS の PWA 制限に注意（プッシュ通知非対応等）
