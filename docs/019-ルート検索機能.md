# 019: ルート検索機能

**フェーズ**: Phase 2-A
**優先度**: 高
**依存**: 015（交通テーブル）, 017（Google Maps統合）, 018（交通データ投入後）
**参照**: Phase2-Requirements.md セクション 3.2, 3.3, 3.4, 10.1

---

## 概要

出発地→目的地のルート検索機能を実装。Google Maps Directions API + 地域交通データベースを組み合わせ、最大3ルートを提示する。

---

## Todo

### ルート検索 API

- [x] `POST /api/route/search/route.ts` — ルート検索エンドポイント
  - from/to の座標解決（Geocoding / hospitals テーブル）
  - Google Maps Directions API（driving / transit）
  - 地域交通マッチング（逆ジオコーディングで市町村判定）
  - 最大3ルート返却
- [x] ルート検索結果の型定義（`src/types/route.ts`）

### 地域交通マッチングロジック

- [x] `src/lib/transportMatcher.ts` — 出発地の市町村からデマンド交通・タクシー・送迎バスを検索
  - 車椅子フィルタリング対応

### 出発地入力コンポーネント

- [x] `src/components/Route/LocationInput.tsx`
  - GPS自動取得 / 住所テキスト入力 / 地区名選択（14市町村）
  - GPS 拒否時のフォールバック（住所入力に切替）

### ルート検索結果ページ

- [x] `/route/page.tsx` — ルート検索結果表示（Suspense ラップ）
  - URLパラメータ: `to=hospital_id&name=病院名`
- [x] `src/components/Route/RouteCard.tsx` — ルートカード
  - 交通手段アイコン（🚗🚌🚐🚕🚶🏥）+ 名称
  - 所要時間・距離・料金・手順・注意事項
  - 電話予約ボタン（56px）/ Web予約 / Google Maps リンク
- [ ] `src/components/Route/RouteMap.tsx` — Google Maps 埋め込みルート表示（将来対応）

### 既存ページへの導線追加

- [x] `/hospital/[id]` — 「🚌 ここへの行き方を調べる」ボタン追加
- [ ] `/results` — 病院リスト項目に「行き方」ボタン追加（025チケットで対応）
- [ ] `/search/results` — 検索結果に「行き方」ボタン追加（025チケットで対応）

### コスト最適化

- [ ] Directions API 結果のキャッシュ（将来対応）
- [ ] 「交通手段がわからない場合は 0265-XX-XXXX にお電話ください」の案内を常時表示

### 確認

- [ ] 各ルート表示パターンの動作確認
- [ ] 電話予約ボタンのタップ動作確認（モバイル）
- [x] `npm run build` でエラーなし確認

---

## 技術メモ

- Google Maps Directions API: $5/1,000リクエスト → キャッシュで削減
- 出発地の GPS 取得は `navigator.geolocation.getCurrentPosition()`
- 電話予約ボタンは 56px 高さ（Phase 1 の 48px より大きい特大サイズ）
- ルートマップは動的インポートでバンドルサイズ対策
