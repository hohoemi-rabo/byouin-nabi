# 027: 緊急時ガイド・統計ダッシュボード・検索ログ

**フェーズ**: Phase 2-C
**優先度**: 低〜中
**依存**: 015（search_logs テーブル）, 016（緊急度判定）
**参照**: Phase2-Requirements.md セクション 8.4, 10.5, 11.1

---

## 概要

緊急時ガイドページの新規作成、匿名検索ログの記録、管理画面ダッシュボードの統計強化。

---

## Todo

### 緊急時ガイドページ

- [x] `/emergency/page.tsx` — 119番ボタン・応急処置ガイド・救急対応病院リスト・免責事項

### 匿名検索ログ

- [x] `POST /api/logs/route.ts` — 検索ログ記録API（log_type, search_data, area）
- [x] `SearchLogger` コンポーネント — 各ページに配置可能な汎用ログ送信（useRef で1回制限）
- [ ] 各検索ページへの SearchLogger 配置（将来対応）

### 管理画面ダッシュボード強化

- [x] 統計カード7種（病院・市町村・診療科・交通・施設・ユーザー・今月検索回数）
- [x] Promise.all で全統計を並列取得（supabaseAdmin 使用）
- [x] クイックアクションに交通・施設管理リンク追加
- [ ] 症状TOP5・エリア別分布（ログデータ蓄積後に対応）

### 確認

- [ ] 動作確認
- [x] `npm run build` でエラーなし確認

---

## 技術メモ

- search_logs は INSERT のみ Public（誰でも記録可能）、SELECT は Admin のみ
- ダッシュボードは Server Component を維持（クライアント JS 0B を目標）
- 検索ログ送信は `navigator.sendBeacon()` or `fetch` のバックグラウンド実行
- 統計は SQL の集計クエリで実行（将来的にチャート化を検討）
