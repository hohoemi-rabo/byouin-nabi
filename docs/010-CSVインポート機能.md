# 010 - CSV/Excelインポート機能

## 概要
CSV/Excelファイルから病院データを一括インポートする機能を実装します。

## 目的
- 大量の病院データを効率的に登録
- データフォーマットのバリデーション
- エラーハンドリング

## タスク

### パッケージインストール
- [x] papaparse（CSV解析）のインストール
- [x] xlsx（Excel解析）のインストール
- [x] 型定義のインストール

### インポートUI
- [x] /admin/hospitals/import ページ作成
- [x] ファイルアップロードフォーム
- [ ] プレビュー機能（オプション、実装せず）
- [x] インポート実行ボタン
- [x] 進行状況表示（ローディングモーダル + 完了モーダル）

### Server Action 実装
- [x] importHospitals アクション作成（/src/app/admin/actions.ts）
- [x] CSVファイルの解析
- [x] Excelファイルの解析
- [x] データバリデーション
- [x] バルクインサート処理（全削除 + 新規挿入）

### エラーハンドリング
- [x] ファイル形式チェック
- [x] データバリデーション
- [x] エラー行の報告
- [x] 部分的な成功処理

### テンプレートダウンロード
- [x] CSVテンプレートの作成（UTF-8 BOM対応）
- [x] ダウンロード機能（登録データのCSVエクスポート機能として実装）

## CSVテンプレート形式

```csv
病院名,診療科,住所,電話番号,市町村,診療時間,Google Maps URL,備考
〇〇病院,"内科,外科",長野県飯田市〇〇町1-2-3,0265-XX-XXXX,飯田市,月-金 9:00-17:00,https://maps.google.com/...,駐車場あり
```

## 実装例

```typescript
// src/app/admin/hospitals/import/actions.ts
'use server';

import { parse } from 'papaparse';
import * as XLSX from 'xlsx';
import { supabase } from '@/lib/supabase';
import { verifyAdminSession } from '@/lib/auth';

interface ImportResult {
  success: number;
  errors: Array<{ row: number; message: string }>;
}

export async function importHospitals(
  formData: FormData
): Promise<ImportResult> {
  const session = await verifyAdminSession();
  if (!session) {
    throw new Error('管理者権限が必要です');
  }

  const file = formData.get('file') as File;
  if (!file) {
    throw new Error('ファイルが選択されていません');
  }

  const fileName = file.name.toLowerCase();
  let parsedData: any[] = [];

  // ファイル形式による解析
  if (fileName.endsWith('.csv')) {
    const text = await file.text();
    const result = parse(text, {
      header: true,
      skipEmptyLines: true,
    });
    parsedData = result.data;
  } else if (fileName.endsWith('.xlsx') || fileName.endsWith('.xls')) {
    const buffer = await file.arrayBuffer();
    const workbook = XLSX.read(buffer);
    const sheetName = workbook.SheetNames[0];
    const worksheet = workbook.Sheets[sheetName];
    parsedData = XLSX.utils.sheet_to_json(worksheet);
  } else {
    throw new Error('対応していないファイル形式です（CSV, XLSXのみ）');
  }

  const result: ImportResult = {
    success: 0,
    errors: [],
  };

  // データのバリデーションと挿入
  for (let i = 0; i < parsedData.length; i++) {
    const row = parsedData[i];
    const rowNumber = i + 2; // ヘッダー行を考慮

    try {
      // バリデーション
      if (!row['病院名'] || !row['住所'] || !row['電話番号']) {
        throw new Error('必須項目が不足しています');
      }

      const hospitalData = {
        name: row['病院名'],
        category: row['診療科']
          ? row['診療科'].split(',').map((s: string) => s.trim())
          : [],
        address: row['住所'],
        tel: row['電話番号'],
        city: row['市町村'] || '',
        opening_hours: row['診療時間'] || null,
        google_map_url: row['Google Maps URL'] || null,
        note: row['備考'] || null,
      };

      // データ挿入
      const { error } = await supabase
        .from('hospitals')
        .insert(hospitalData);

      if (error) {
        throw error;
      }

      result.success++;
    } catch (error) {
      result.errors.push({
        row: rowNumber,
        message: error instanceof Error ? error.message : '不明なエラー',
      });
    }
  }

  return result;
}
```

```typescript
// src/app/admin/hospitals/import/page.tsx
'use client';

import { useState } from 'react';
import { importHospitals } from './actions';

export default function ImportPage() {
  const [file, setFile] = useState<File | null>(null);
  const [isImporting, setIsImporting] = useState(false);
  const [result, setResult] = useState<any>(null);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!file) return;

    setIsImporting(true);
    try {
      const formData = new FormData();
      formData.append('file', file);

      const importResult = await importHospitals(formData);
      setResult(importResult);
    } catch (error) {
      alert(error instanceof Error ? error.message : 'インポートに失敗しました');
    } finally {
      setIsImporting(false);
    }
  };

  return (
    <div className="max-w-2xl">
      <h1 className="text-2xl font-bold mb-6">病院データ一括インポート</h1>

      <div className="mb-6">
        <a
          href="/templates/hospitals_template.csv"
          className="text-primary underline"
          download
        >
          📥 CSVテンプレートをダウンロード
        </a>
      </div>

      <form onSubmit={handleSubmit} className="space-y-6">
        <div>
          <label className="block font-medium mb-2">
            ファイルを選択（CSV, XLSX）
          </label>
          <input
            type="file"
            accept=".csv,.xlsx,.xls"
            onChange={(e) => setFile(e.target.files?.[0] || null)}
            className="border rounded px-3 py-2 w-full"
          />
        </div>

        <button
          type="submit"
          disabled={!file || isImporting}
          className="bg-primary text-white px-6 py-2 rounded hover:bg-primary-dark disabled:opacity-50"
        >
          {isImporting ? 'インポート中...' : 'インポート実行'}
        </button>
      </form>

      {result && (
        <div className="mt-6 p-4 border rounded">
          <h2 className="font-bold text-lg mb-2">インポート結果</h2>
          <p className="text-green-600">✓ 成功: {result.success}件</p>
          {result.errors.length > 0 && (
            <div className="mt-4">
              <p className="text-red-600 font-bold">
                ✗ エラー: {result.errors.length}件
              </p>
              <ul className="mt-2 space-y-1 text-sm">
                {result.errors.map((err: any, idx: number) => (
                  <li key={idx} className="text-red-600">
                    {err.row}行目: {err.message}
                  </li>
                ))}
              </ul>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
```

## package.json 追加

```json
{
  "dependencies": {
    "papaparse": "^5.4.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.14"
  }
}
```

## 受け入れ基準
- [x] CSVファイルからインポートできる
- [x] Excelファイルからインポートできる
- [x] バリデーションエラーが適切に報告される
- [x] 成功件数とエラー件数が表示される
- [x] エラー行が特定できる
- [x] テンプレートファイルがダウンロードできる（登録データCSVダウンロード機能）
- [x] インポート前の確認モーダル表示
- [x] インポート中のローディング表示
- [x] インポート完了モーダル表示
- [x] 既存データ全削除 + 新規データインポート（フル置換方式）

## 依存関係
- 009-病院CRUD機能

## 関連ファイル
- `/home/masayuki/NextJs/byouin-nabi/src/app/admin/hospitals/import/page.tsx`
- `/home/masayuki/NextJs/byouin-nabi/src/app/admin/hospitals/import/actions.ts`
- `/home/masayuki/NextJs/byouin-nabi/public/templates/hospitals_template.csv`

## 備考
- 大量データ（100件以上）の場合はバッチ処理を検討
- インポート中のプログレスバー表示は将来対応
- 重複チェック機能は将来対応

## 実装完了日
**2025年11月19日**

## 実装内容
- CSVテンプレートではなく、登録済みデータをCSVでダウンロードする機能を実装
- ユーザーは登録データをダウンロード → 編集 → インポートのワークフロー
- インポート時は既存データを全削除してから新規データを挿入（フル置換方式）
- ConfirmModal、LoadingSpinner、完了モーダルによるリッチなUI
- UTF-8 BOM対応でExcelでの文字化け防止
