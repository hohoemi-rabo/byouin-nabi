# 021: マイページ・かかりつけ医・履歴

**フェーズ**: Phase 2-B
**優先度**: 中
**依存**: 015（ユーザー関連テーブル）, 020（Supabase Auth 導入後）
**参照**: Phase2-Requirements.md セクション 6.3, 6.4, 6.6

---

## 概要

ログインユーザー向けのマイページを構築。かかりつけ医登録、受診履歴、設定機能を提供する。

---

## Todo

### マイページ

- [x] `/mypage/page.tsx` — マイページトップ（Client Component、AuthGuard でラップ）
  - プロフィール情報サマリー
  - かかりつけ医一覧（最大5件）
  - 受診履歴（直近10件）
  - リマインダー一覧（→ 022チケット）
  - 家族見守り設定（→ 023チケット）
  - 設定リンク

### かかりつけ医登録

- [x] `src/components/User/FavoriteButton.tsx` — ⭐ トグルボタン
  - ログイン時: 登録/解除をワンタップ
  - 未ログイン時: 非表示
  - 登録上限5件チェック
- [x] `/hospital/[id]` ページに「⭐ かかりつけ医に登録」ボタン追加
- [x] マイページにかかりつけ医一覧表示
  - 各病院名 + 📞 電話リンク + 解除ボタン

### API ルート

- [x] `GET/POST/DELETE /api/user/favorites/route.ts`
  - GET: かかりつけ医一覧取得（hospitals JOIN、sort_order順）
  - POST: 登録（上限5件チェック、UNIQUE制約）
  - DELETE: 解除
- [x] `GET /api/user/history/route.ts` — 検索履歴取得（直近10件、hospitals JOIN）
- [x] `POST /api/user/history/route.ts` — 検索履歴記録

### 受診履歴

- [x] マイページに履歴一覧表示（直近10件、病院詳細へのリンク付き）
- [ ] 結果画面で病院をタップした際に自動記録（将来対応）

### 受診先レコメンドへの反映

- [ ] 結果ページの病院リスト: かかりつけ医は ⭐ バッジ付きで上位表示（025チケットで対応）
- [ ] スコアリング: かかりつけ医登録済み → +50 のスコア加算（025チケットで対応）

### 設定ページ

- [x] `/mypage/settings/page.tsx` — 設定画面
  - プロフィール編集リンク
  - ログアウトボタン

### 確認

- [ ] かかりつけ医の登録・解除・上限チェック確認
- [ ] 受診履歴の表示確認
- [ ] 未ログインユーザーでの動作確認（FavoriteButton非表示）
- [x] `npm run build` でエラーなし確認

---

## 技術メモ

- かかりつけ医は `favorite_facilities` テーブル（`UNIQUE(user_id, hospital_id)`）
- 検索履歴は `search_history` テーブル（`search_params` は JSONB）
- ゲストユーザー（未ログイン）は Phase 1 と同等の機能を維持
- かかりつけ医の並び替えは `sort_order` カラムで管理
