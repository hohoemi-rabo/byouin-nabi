# 024: お出かけナビ

**フェーズ**: Phase 2-C
**優先度**: 中
**依存**: 015（facilities テーブル）, 017（Google Maps）, 019（ルート検索）
**参照**: Phase2-Requirements.md セクション 5

---

## 概要

病院以外の外出先（スーパー・役所・銀行等）への交通手段を案内する「お出かけナビ」機能。

---

## Todo

### 施設管理画面（管理者向け）

- [x] `AdminSidebar` に「🏪 施設管理」メニュー項目を追加
- [x] `/admin/facilities/page.tsx` — 施設一覧（カテゴリフィルター + CRUD）
- [x] `/admin/facilities/new/page.tsx` — 施設新規登録
- [x] `/admin/facilities/[id]/edit/page.tsx` — 施設編集
- [x] `src/components/Admin/FacilityForm.tsx` — 登録/編集共用フォーム（座標取得ボタン付き）
- [x] `src/app/admin/facility-actions.ts` — CRUD + CSVインポート/エクスポート
- [x] `/admin/facilities/import/page.tsx` — CSVインポート画面

### 施設 API

- [x] `GET /api/facilities/route.ts` — 施設一覧取得（category/city フィルタ）

### お出かけナビ ユーザー画面

- [x] `/outing/page.tsx` — カテゴリ選択画面（Server Component、5カテゴリ）
- [x] `/outing/[category]/page.tsx` — カテゴリ別施設一覧（市町村フィルタ付き）
- [x] `src/components/Outing/FacilityCard.tsx` — 施設カード（電話・Web・行き方ボタン）

### ホームページ改修

- [x] トップページのCTAに「🚶 お出かけナビ」ボタンを3番目として追加（ヒーロー + 下部CTA）

### 初期データ投入

- [x] 飯田市中心部の主要施設データ16件を準備（CSV）・インポート・座標取得

### 確認

- [ ] カテゴリ選択 → 施設一覧 → ルート検索の一連フロー確認
- [ ] 管理画面での施設 CRUD 確認
- [x] `npm run build` でエラーなし確認
- [ ] レスポンシブ表示確認
- [ ] `npm run build` でエラーなし確認

---

## 技術メモ

- 施設データは `facilities` テーブル（`hospitals` とは別）
- 「医療・福祉」カテゴリは既存の病院検索へリンク（データ重複を避ける）
- Phase 1 のコンポーネント（QuestionOption, HospitalListItem, Accordion）を再利用
- 飯田市中心部から開始、ユーザーフィードバックで拡充
