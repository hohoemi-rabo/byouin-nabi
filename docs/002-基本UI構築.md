# 002 - 基本UI構築

## 概要
シニア層向けに最適化された基本UIコンポーネントとレイアウトを構築します。

## 目的
- アクセシビリティに配慮した共通コンポーネントの作成
- レスポンシブなレイアウトの実装
- 文字サイズ切替機能の実装

## タスク

### レイアウトコンポーネント
- [x] Header コンポーネント作成
  - [x] ロゴ・サービス名表示
  - [x] 文字サイズ切替ボタン
- [x] Footer コンポーネント作成
  - [x] 利用規約リンク
  - [x] お問い合わせリンク
- [x] RootLayout 更新（グローバルレイアウト）

### 共通コンポーネント
- [x] Button コンポーネント
  - [x] 最小タップ領域 48px × 48px
  - [x] variant（primary, secondary）対応
- [x] FontSizeToggle コンポーネント
  - [x] 標準モード（18px）
  - [x] 大文字モード（24px）
  - [x] LocalStorageに設定を保存
- [x] LoadingSpinner コンポーネント

### スタイリング設定
- [x] globals.css の更新
  - [x] カラーパレット定義（CSS変数）
  - [x] フォントサイズ変数
  - [x] アクセシビリティ対応
- [x] Tailwind設定の調整
  - [x] カスタムカラー追加
  - [x] フォントサイズユーティリティ

### ホームページUI
- [x] ランディングページのデザイン
- [x] サービス説明セクション
- [x] 「症状から病院を探す」CTA ボタン

## 実装例

```typescript
// src/components/Common/Button.tsx
interface ButtonProps {
  children: React.ReactNode;
  variant?: 'primary' | 'secondary';
  onClick?: () => void;
  type?: 'button' | 'submit';
  disabled?: boolean;
}

export default function Button({
  children,
  variant = 'primary',
  ...props
}: ButtonProps) {
  const baseClass = "px-6 py-3 rounded-lg min-h-[48px] min-w-[48px] font-medium transition";
  const variantClass = variant === 'primary'
    ? 'bg-primary text-white hover:bg-primary-dark'
    : 'bg-gray-100 text-foreground hover:bg-gray-200';

  return (
    <button
      className={`${baseClass} ${variantClass}`}
      {...props}
    >
      {children}
    </button>
  );
}
```

## 受け入れ基準
- [x] ヘッダー・フッターが全ページで表示される
- [x] 文字サイズ切替が正常に動作する
- [x] ボタンのタップ領域が48px以上ある
- [x] 高コントラスト配色が適用されている
- [x] モバイル・デスクトップで適切に表示される

## 依存関係
- 000-プロジェクトセットアップ

## 関連ファイル
- `/home/masayuki/NextJs/byouin-nabi/src/app/layout.tsx`
- `/home/masayuki/NextJs/byouin-nabi/src/app/globals.css`
- `/home/masayuki/NextJs/byouin-nabi/src/components/Common/`
- `/home/masayuki/NextJs/byouin-nabi/tailwind.config.ts`

## 備考
- WCAG AA レベルのコントラスト比を確保
- フォントは Noto Sans JP の使用を推奨
- ダークモードは初期リリースでは対応しない（将来対応）
