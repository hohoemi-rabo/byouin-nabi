# 025: 結果ページ強化・スコアリング

**フェーズ**: Phase 2-B
**優先度**: 高
**依存**: 016（Gemini AI統合）, 017（Google Maps）, 020（ユーザー認証 — かかりつけ医スコア用）
**参照**: Phase2-Requirements.md セクション 4.3, 4.4

---

## 概要

Phase 1 の結果ページ（`/results`）を Phase 2 仕様に強化。AI緊急度判定をメインセクション化し、病院リストをスコアリング順に変更。各病院に「行き方」ボタンを追加。

---

## Todo

### 結果ページレイアウト変更

- [ ] `/results/page.tsx` の表示構成を Phase 2 仕様に変更:
  1. 🔴🟡🟢 緊急度判定（最上部に大きく表示）← 016 の UrgencyBadge
  2. 推奨される診療科（Phase 1 の RecommendedDepartments 維持 + AI推奨を統合）
  3. 症状まとめ（Phase 1 の SymptomDescription 維持）
  4. 対応病院リスト（スコア順に変更）
- [ ] Phase 1 の AI診断アコーディオンを削除（緊急度判定に統合済み）

### 病院スコアリング

- [ ] `src/lib/hospitalScoring.ts` — スコアリングロジック
  - 推奨診療科の一致: +100（必須条件）
  - ユーザー現在地からの距離: +50（近い）〜 +10（遠い）
  - 診療時間の適合: +30（希望日時に営業中）
  - オンライン診療対応: +20（移動困難な場合）
  - かかりつけ医登録: +50（ログインユーザーのみ）
  - 救急対応: +40（緊急度が「緊急」の場合のみ）
- [ ] 結果ページの病院リストをスコア順でソート

### 病院リスト項目の強化

- [ ] `HospitalListItem` に以下を追加:
  - 「🚌 行き方」ボタン → 019 のルート検索への導線
  - オンライン診療対応マーク（💻 バッジ）
  - 営業中/営業時間外の表示（現在時刻と `hospital_schedules` で判定）
  - かかりつけ医 ⭐ バッジ（ログインユーザーのみ）
- [ ] 緊急度が「緊急」の場合: 救急対応病院のみ表示 + 119番ボタン

### 検索ページフィルター追加

- [ ] `/search/page.tsx` にフィルター追加:
  - オンライン診療対応（チェックボックス）
  - バリアフリー対応（チェックボックス）
  - 救急対応（チェックボックス）
  - 駐車場あり（チェックボックス）
- [ ] `/api/search/route.ts` に新フィルター対応を追加

### 確認

- [ ] 緊急度判定の3パターン表示確認
- [ ] スコアリング順の病院リスト表示確認
- [ ] 「行き方」ボタンからルート検索への遷移確認
- [ ] 検索フィルターの動作確認
- [ ] ゲストユーザー（かかりつけ医なし）での表示確認
- [ ] `npm run build` でエラーなし確認

---

## 技術メモ

- スコアリングはクライアントサイドで実行（API レスポンス後に計算）
- 距離計算: Google Maps Distance Matrix API or 市町村一致で簡易判定
- 営業中判定: 現在の曜日・時刻と `hospital_schedules` を比較
- Phase 1 の既存コンポーネントは可能な限り維持（破壊的変更を最小化）
